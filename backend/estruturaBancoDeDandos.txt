Mysql

CREATE DATABASE IF NOT EXISTS note_api;

USE note_api;

CREATE TABLE IF NOT EXISTS  notes
(
	id INT NOT NULL AUTO_INCREMENT,
	note VARCHAR(600) NOT NULL,
	collor VARCHAR(50) NOT NULL,
	PRIMARY KEY(id)
);